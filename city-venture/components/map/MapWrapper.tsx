import React from 'react';
import { View } from 'react-native';

export const MapView = (props: any) => <View />;
export const Marker = (props: any) => <View />;
export const Callout = (props: any) => <View />;
export const PROVIDER_GOOGLE = 'google';
