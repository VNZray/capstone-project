import PageContainer from "@/src/components/PageContainer";
import { Typography } from "@mui/joy";

const Notification = () => {
  return (
    <PageContainer>
      <Typography level="h1">Notification</Typography>
    </PageContainer>
  );
};

export default Notification;
