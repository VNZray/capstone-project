/**
 * Unified Subscription Page
 * Subscription management - shared across all business types
 */

// Re-export from the local subscription folder
export { default } from './subscription/Subscription';
