import Typography from "@/src/components/Typography";

const Shop: React.FC = () => {
  return (
    <>
      <Typography.Title size="lg">Shop</Typography.Title>
    </>
  );
};

export default Shop;
